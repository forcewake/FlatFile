namespace FluentFiles.FixedLength
{
    /// <summary>
    /// The types of padding a fixed-length field may have.
    /// </summary>
    public enum Padding
    {
        /// <summary>
        /// A field is padded to the left.
        /// </summary>
        Left,

        /// <summary>
        /// A field is padded to the right.
        /// </summary>
        Right
    }
}